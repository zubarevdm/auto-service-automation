# -*- coding: utf-8 -*-
"""Все настраиваемые параметры системы. Правьте под конкретный сервис здесь."""

# --- Пороги сегментации ---
# Основной критерий — доля от ЛИЧНОГО среднего интервала клиента между визитами.
SLEEP_RATIO = (1.5, 2.5)        # «спящие»
LEAVE_RATIO = (2.5, 4.0)        # «уходящие»
# Fallback в днях — для клиентов с одним визитом (личный интервал неизвестен).
SLEEP_DAYS = (180, 270)
LEAVE_DAYS = (270, 450)
LOST_DAYS = 450                 # дальше — «потерянные»
MIN_VISITS_REGULAR = 3          # «уходящим» считаем только бывшего регулярным

DEFAULT_DAILY_RUN_KM = 45      # средний дневной накат, если нет двух точек пробега
MILEAGE_TRIGGER_KM = 9000      # накат с последнего визита, после которого пора на ТО

SEASON_MONTHS = {4: "летнюю", 10: "зимнюю", 11: "зимнюю"}   # месяцы переобувки
TIRE_KEYWORDS = ("шин", "резин", "колес", "переобув")

# --- Каденция: (номер шага, сдвиг в днях от старта цепочки) ---
CADENCE = [(1, 0), (2, 6), (3, 13)]
CADENCE_LOST = [(1, 0)]         # потерянным — одно касание, не больше
QUARANTINE_DAYS = 45            # карантин после завершённой цепочки

# --- Атрибуция и деньги ---
ATTRIBUTION_WINDOW_DAYS = 14    # заезд в течение N дней после касания = возвращённый
COMMISSION = 0.20               # ваша доля от возвращённой выручки (пилот)

# --- Прогноз для аудита (консервативные конверсии в заезд) ---
CONVERSION = {
    "mileage": 0.11, "seasonal": 0.10,
    "sleeping": 0.07, "leaving": 0.04, "lost": 0.015,
}
SHOW_RATE = 0.80                # доходимость записавшихся

SEGMENT_TITLES = {
    "mileage": "По пробегу", "seasonal": "Сезонные",
    "sleeping": "Спящие", "leaving": "Уходящие", "lost": "Потерянные",
}
SEGMENT_ORDER = ["mileage", "seasonal", "sleeping", "leaving", "lost"]

# --- Офферы (то, что реально готов дать владелец сервиса) ---
OFFERS = {
    "sleeping": "бесплатная диагностика по 10 точкам",
    "leaving":  "скидка 15% на все работы, действует 14 дней",
    "lost":     "замена масла в подарок при любом ремонте",
    "seasonal": "переобувка без очереди, к точному времени",
    "mileage":  "замена масла и фильтров за 40 минут",
}

# --- Реквизиты сервиса (подпись в сообщениях) ---
SERVICE_NAME = "Автосервис «Мотор»"
SERVICE_PHONE = "+7 (900) 000-00-00"
SERVICE_ADDRESS = "ул. Примерная, 1"
